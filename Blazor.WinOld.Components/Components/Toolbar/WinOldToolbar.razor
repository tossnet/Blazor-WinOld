@namespace Blazor.WinOld.Components
@inherits WinOldComponentBase
@implements IAsyncDisposable

<div role="toolbar"
     @ref="_rootRef"
     class="toolbar-win @GetComponentClass() @Class"
     style="@Style"
     @attributes="Attributes">

    @if (Mode == ToolbarOverflowMode.Collapse)
    {
        <style>
            .toolbar-btn-win.toolbar-btn-collapsed-win .toolbar-btn-label-win { display: none; }
            .toolbar-btn-win.toolbar-btn-collapsed-win { min-width: 32px !important; min-height: 0 !important; }
            .toolbar-btn-win.toolbar-btn-collapsed-win.win-touch { min-width: 44px !important; min-height: 32px !important; }
        </style>
    }

    <CascadingValue Name="Disabled" Value="@(ParentDisabled == true || Disabled)">
        <CascadingValue Name="TouchMode" Value="@IsTouch">
            <CascadingValue Value="this" IsFixed="false">
                @ChildContent
            </CascadingValue>
        </CascadingValue>
    </CascadingValue>
</div>
