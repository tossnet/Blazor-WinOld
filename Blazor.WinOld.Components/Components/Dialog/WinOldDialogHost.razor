@namespace Blazor.WinOld.Components

@inherits WinOldComponentBase

@if (IsVisible)
{
    <div class="dlg-win-backdrop" style="z-index: @_zIndex">
        <div @ref="_windowRef"
             role="dialog"
             aria-modal="true"
             aria-labelledby="@_titleId"
             class="dlg-win @GetDialogClass() @GetMaximizedClass()" style="@GetDialogSizeStyle()">
            <WinOldTitleBar @ref="_titleBar"
                            BarClass="@GetTitleBarClass()"
                            TextClass="@GetTitleBarTextClass()"
                            ControlsClass="@GetTitleBarControlsClass()"
                            Title="@Options.Title"
                            TitleId="@_titleId"
                            OnClose="HandleCancel"
                            ShowMaximizeButton="Options.MaxButton"
                            OnToggleMaximize="ToggleMaximize" />

            <div class="dlg-win-body @GetDialogBodyClass()">
                @Options.ChildContent
            </div>

            <div class="actions">
                <WinOldButton OnClick="HandleOk"
                              Appearance="Options.Appearance"
                              Default="true">@Options.OkButtonText</WinOldButton>
                @if (!string.IsNullOrEmpty(Options.CancelButtonText))
                {
                    <WinOldButton OnClick="HandleCancel"
                                  Appearance="Options.Appearance"
                                  Style="margin-left: 8px;">@Options.CancelButtonText</WinOldButton>
                }
            </div>
        </div>
    </div>
}

<DraggableWindow @ref="_draggable" />